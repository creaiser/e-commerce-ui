'use client';

import { ProductType } from '@/types';

export const ProductCard = ({ product }: { product: ProductType }) => {
  return (
    <div className="">
      <div className="relative aspect-[3/1] mb-12"></div>
    </div>
  );
};
